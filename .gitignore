# 의존성 (재설치 가능, 올리지 않음)
node_modules/
poc/node_modules/

# poc — 팀원 테스트 키트. 공개 레포 제외(팀 결정 9562b9d). 비밀(.env) 노출 방지 + zip으로 직접 배포.
poc/
poc.zip

# 비밀정보 — 절대 커밋 금지 (인스타 토큰, API 키 등)
.env
.env.*
!.env.example
*.local
secrets/
**/secrets/

# 비공개 문서 — 실제 인터뷰이 식별정보 등 공개 부적합 (로컬에만 보관)
docs/product/instagram_interview_PRD_synthesis.md
docs/private/

# Next.js / 빌드 산출물
.next/
out/
build/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Supabase 로컬 CLI
supabase/.branches/
supabase/.temp/

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로그
*.log
npm-debug.log*

# 대용량 바이너리 (다운로드로 재취득 가능, 올리지 않음)
poc/cloudflared

# 로컬 런타임 DB / 워크스페이스 파일DB — 로컬 전용, 커밋 금지
.data/
